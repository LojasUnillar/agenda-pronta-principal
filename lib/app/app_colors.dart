import 'package:flutter/material.dart';

/// Define a paleta de cores centralizada do aplicativo.
class AppColors {
  // Cores da Marca
  static const Color brandBlue = Color(0xFF00107C);
  static const Color bgLight = Color(0xFFEAEAEA);

  // Cores Neutras
  static const Color black = Color(0xFF000000);
  static const Color darkGrey = Color(0xFF111111);
  static const Color mediumGrey = Color(0xFF8C8C8C);
  static const Color white = Color(0xFFFFFFFF);

  // Variações e Semânticas (Opcional, para uso futuro)
  static const Color error = Color(0xFFB00020);
  static const Color success = Color(0xFF00C853);
}
